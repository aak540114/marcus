{
  "kanban": {
    "provider": "kanboard"
  },
  "ai": {
    "provider": "${MARCUS_AI_PROVIDER}",
    "anthropic_api_key": "${CLAUDE_API_KEY}",
    "claude_cli_model": "${MARCUS_CLAUDE_CLI_MODEL}"
  },
  "transport": {
    "type": "http",
    "http": {
      "host": "${MARCUS_NATIVE_BIND_HOST}",
      "port": 4298,
      "path": "/mcp"
    }
  }
}
